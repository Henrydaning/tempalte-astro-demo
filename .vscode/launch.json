{
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch React测试",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}